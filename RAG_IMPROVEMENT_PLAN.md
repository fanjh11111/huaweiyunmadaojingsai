# 基于 RAG 智能体的发动机处置方案生成优化思路

## 一、优化目标

当前项目已经具备发动机故障预测、故障段识别、部件风险分析和前端可视化展示能力。后续可以在此基础上引入基于 RAG 的智能体，用于根据发动机异常结果自动生成维修处置建议。

嵌入到现有预测报告或故障分析报告中，作为“AI 维修处置建议”模块。这样可以直接承接模型预测结果，形成“故障预测 -> 依据检索 -> 处置建议 -> 报告展示”的完整闭环。

优化重点不追求知识库全面覆盖，先接入少量高价值文档，完成从故障识别到维修建议生成的闭环；后续再逐步扩展文档范围、规则覆盖和开放问答能力。

## 二、核心思路总结


1. 在现有故障预测结果基础上，引入一个报告内的智能处置建议模块。
2. 使用 RAG 架构，将维修手册、机场处理手册和相关民航安全规章作为知识来源。
3. 当模型识别出发动机参数异常、部件故障或风险等级升高时，智能体检索相关条款、维修流程和处置建议。
4. 智能体结合当前故障信息，生成面向地面维修人员的处理方案。
5. 初期只做几个典型场景，例如发动机参数超限、液压系统泄漏、偏离厂家手册或适航指令要求等，后续逐步扩展。
6. 普通问答助手作为第二阶段能力，在报告建议稳定后再增加。

## 三、第一版功能形态选择

第一版建议采用“报告内自动生成处置建议”的形式，而不是普通聊天问答。

原因如下：

- 与现有系统流程更一致：当前项目已经有上传数据、模型预测、预测报告和故障分析页面，RAG 模块可以直接接在预测结果之后。
- 展示效果更稳定：比赛演示时不需要评委主动提问，系统可以自动展示异常判断、处置步骤和参考依据。
- 输入更可控：报告生成使用后端已有的故障部件、异常特征、风险等级和置信度，避免开放问答因为知识库不完整导致回答不稳定。
- 更符合“先解决有无问题”：先做固定结构的维修建议，再逐步扩展成可追问、可自由问答的智能助手。

第一版建议在 `PredictReport.vue` 或 `FaultAnalysisReport.vue` 中新增模块：

```text
AI 维修处置建议
├─ 异常判断
├─ 风险等级
├─ 建议检查步骤
├─ 是否建议放行
├─ 参考依据
└─ 注意事项
```

普通问答可以作为第二阶段能力，例如在报告旁边增加“追问”按钮，支持维修人员继续询问“为什么判断为该部件异常”“能否继续放行”“还需要检查哪些项目”等问题。

## 四、首批可做的典型场景

初期建议先做 3 到 5 个常见且容易展示效果的场景。

### 1. 发动机参数超限

输入示例：

- 排气温度 EGT 超过阈值；
- 发动机转速异常；
- 振动值持续升高；
- 滑油温度或压力异常。

输出目标：

- 判断异常类型；
- 给出是否需要停场检查的初步建议；
- 给出检查部件和检查顺序；
- 提醒参考维修手册和厂家限制要求。

### 2. 液压系统泄漏

输入示例：

- 液压压力下降；
- 液压油量异常；
- 维护记录中出现泄漏描述。

输出目标：

- 判断泄漏风险等级；
- 建议检查管路、接头、作动筒等部位；
- 提醒是否需要隔离系统或执行进一步检查。

### 3. 偏离适航指令或厂家手册要求

输入示例：

- 发动机参数超过厂家手册限制；
- 检查结果与适航指令要求不一致；
- 维修流程未满足手册要求。

输出目标：

- 提醒存在合规风险；
- 建议复核适航指令、厂家维修手册和放行标准；
- 输出“不应直接放行，应由有资质人员复核”的保守建议。

### 4. 故障预测高风险

输入示例：

- LSTM 模型预测某部件故障概率高；
- 连续故障段置信度较高；
- 多个关键特征同时异常。

输出目标：

- 将模型结果翻译成维修人员容易理解的描述；
- 结合知识库生成检查建议；
- 给出下一步维护动作。

## 五、RAG 知识库内容

初期知识库可以包含以下几类文档。

### 1. 维修手册

用于提供发动机部件检查、维护流程、参数限制和故障排查方法。

重点内容：

- 发动机参数限制；
- 常见故障排查流程；
- 部件检查要求；
- 维修后复查要求。

### 2. 机场处理手册

用于提供飞机落地后、航班周转期间、地面保障过程中的处置流程。

重点内容：

- 航后检查流程；
- 异常情况上报流程；
- 地面保障协同流程；
- 航班延误或停场处置建议。

### 3. 民航安全和事故调查相关规定

可作为合规提醒和安全边界来源。

可纳入的方向：

- 《民用航空器事故调查规定》；
- 《民用航空安全信息管理规定》；
- 与发动机参数超限、液压泄漏、适航指令偏离、厂家手册偏离相关的安全信息管理要求。

注意：法规和适航文件可能更新，实际使用时需要以最新官方版本为准。

## 六、总体技术流程

```text
故障预测结果 / 传感器异常
        ↓
整理成结构化故障描述
        ↓
在知识库中检索相关维修手册、机场处置流程、法规条款
        ↓
将检索结果和当前故障信息一起交给大模型
        ↓
生成维修处置方案
        ↓
前端报告展示：风险说明、建议动作、参考依据、注意事项
```

## 七、推荐实现步骤

### 第一步：整理最小知识库

先准备少量文档片段，不需要一开始放入完整手册。

可以先整理为 Markdown 或 TXT 格式，例如：

```text
knowledge_base/
├─ engine_manual/
│  ├─ engine_over_temperature.md
│  ├─ engine_vibration.md
│  └─ oil_pressure_abnormal.md
├─ airport_handbook/
│  ├─ post_flight_check.md
│  └─ ground_maintenance_process.md
└─ regulations/
   ├─ safety_information_management.md
   └─ accident_investigation.md
```

每个文档先写短内容即可，重点是让系统能检索到“相关依据”。

### 第二步：构建向量索引

将知识库文档切分成小段，然后计算向量并存入向量数据库。

初期可以使用轻量方案：

- FAISS；
- Chroma；
- SQLite + 向量扩展；
- 或者直接用简单的本地向量文件。

先做本地版即可，不必马上接复杂数据库。

### 第三步：设计故障输入格式

把后端 `/api/predict` 返回的故障结果整理成结构化 JSON，方便智能体理解。

示例：

```json
{
  "component": "动力涡轮",
  "fault_type": "发动机参数超限",
  "risk_level": "高",
  "confidence": 0.86,
  "abnormal_features": ["排气温度", "振动值"],
  "description": "发动机排气温度和振动值持续升高，模型判断存在动力涡轮异常风险。"
}
```

### 第四步：检索相关资料

根据 `component`、`fault_type`、`abnormal_features` 生成检索问题，例如：

```text
动力涡轮 排气温度超限 振动值升高 航后检查 维修处置
```

系统从知识库中检索最相关的若干段内容。

### 第五步：生成处置方案

将故障信息和检索内容一起交给大模型，要求输出固定结构：

```text
1. 异常判断
2. 风险等级
3. 建议处置步骤
4. 是否建议放行
5. 参考依据
6. 注意事项
```

输出要偏保守，涉及适航、安全和放行判断时，应提示由有资质的维修人员复核。

### 第六步：接入前端报告页面

可以在现有 `PredictReport.vue` 或 `FaultAnalysisReport.vue` 中展示智能体输出。

建议展示内容：

- 故障摘要；
- AI 处置建议；
- 推荐检查步骤；
- 引用的手册或规章片段；
- 风险提示；
- 维修人员确认按钮。

普通问答入口不作为第一版重点，可以等报告建议模块稳定后再加入。

## 八、与现有项目的结合方式

现有项目中可以这样接入：

1. 前端上传 CSV，后端 `/api/predict` 生成故障预测结果；
2. 新增一个 RAG 后端模块，读取故障部件、异常特征、风险等级和置信度；
3. RAG 模块根据故障类型检索维修手册、机场处置流程和法规片段；
4. 大模型结合检索结果生成维修处置方案；
5. 前端预测报告或故障分析页面展示生成结果。
6. 后续再扩展普通问答或追问能力。

可扩展后的结构示例：

```text
hangkong_3D/
├─ backend/
│  ├─ main.py
│  ├─ rag/
│  │  ├─ build_index.py
│  │  ├─ retriever.py
│  │  ├─ agent.py
│  │  └─ knowledge_base/
│  │     ├─ engine_manual/
│  │     ├─ airport_handbook/
│  │     └─ regulations/
│  └─ ...
└─ src/
   ├─ PredictReport.vue
   └─ FaultAnalysisReport.vue
```

## 九、最小可行版本

为了尽快做出效果，可以先实现一个最小版本：

1. 手工整理 3 个知识文档：
   - 发动机排气温度超限；
   - 发动机振动异常；
   - 液压系统泄漏。
2. 使用本地向量库保存这些文档。
3. 写一个简单接口：
   - 输入故障类型和异常参数；
   - 输出处置建议。
4. 在预测报告页面增加“AI 维修建议”模块。

这样可以先证明项目具备“预测 + 依据检索 + 方案生成”的能力，后续再逐步补充更多手册、法规和场景。

## 十、后续扩展方向

报告内建议模块完成后，可以继续扩展为半开放式智能助手：

- 在处置建议下方增加“查看依据”按钮；
- 增加“为什么这样建议”的追问能力；
- 增加“补充检查项”生成能力；
- 最后再升级为独立的维修知识问答助手。

这样可以先保证主流程稳定，再逐步提升交互能力。

## 十一、注意事项

- RAG 输出只能作为辅助建议，不能替代适航放行结论。
- 法规、适航指令和厂家手册必须使用最新有效版本。
- 涉及严重故障、参数超限、泄漏、结构损伤等情况时，建议输出保守结论。
- 前端展示时应区分“模型预测结果”和“维修人员最终判断”。
- 知识库中的每条建议应尽量保留来源，方便追溯。
